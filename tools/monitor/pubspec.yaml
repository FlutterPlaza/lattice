name: lattice_monitor
description: Server health monitoring tool for the Lattice Signal server.
version: 0.1.0
repository: https://github.com/flutterplaza/lattice/tree/main/tools/monitor

environment:
  sdk: ^3.7.0

resolution: workspace

dependencies:
  args: ^2.6.0
  http: ^1.3.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.8
