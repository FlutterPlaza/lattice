name: lattice_deploy
description: Interactive deployment CLI for the Lattice Signal server across cloud platforms.
version: 0.1.0
repository: https://github.com/flutterplaza/lattice/tree/main/tools/deploy

environment:
  sdk: ^3.7.0

resolution: workspace

dependencies:
  args: ^2.6.0
  meta: ^1.16.0
  yaml: ^3.1.3
  path: ^1.9.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.8
