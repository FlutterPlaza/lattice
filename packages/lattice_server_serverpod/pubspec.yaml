name: lattice_server_serverpod
description: Serverpod integration for the Lattice post-quantum Signal protocol server.
version: 0.1.0
repository: https://github.com/flutterplaza/lattice/tree/main/packages/lattice_server_serverpod

environment:
  sdk: ^3.7.0

resolution: workspace

dependencies:
  lattice_crypto: ^0.1.0
  lattice_protocol: ^0.1.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.8
