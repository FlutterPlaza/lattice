name: lattice_server
description: Post-quantum Signal key distribution server for the Lattice protocol.
version: 0.1.0
publish_to: none
repository: https://github.com/flutterplaza/lattice/tree/main/packages/lattice_server

environment:
  sdk: ^3.7.0

resolution: workspace

dependencies:
  lattice_crypto:
    path: ../lattice_crypto
  lattice_protocol:
    path: ../lattice_protocol
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  args: ^2.6.0
  crypto: ^3.0.6
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8
  http: ^1.3.0
