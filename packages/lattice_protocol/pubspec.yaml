name: lattice_protocol
description: SC-AKE and SC-DAKE protocol implementation for the Lattice Signal protocol, based on Hashimoto et al. PQC 2022.
version: 0.1.0
publish_to: none
repository: https://github.com/flutterplaza/lattice/tree/main/packages/lattice_protocol

environment:
  sdk: ^3.7.0

resolution: workspace

dependencies:
  lattice_crypto:
    path: ../lattice_crypto
  crypto: ^3.0.6
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8
